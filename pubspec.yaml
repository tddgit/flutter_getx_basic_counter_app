name: flutter_getx_basic_counter_app

description: Basic counter app with GetX state management

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3
  device_preview: ^0.7.4
  flutter:
    sdk: flutter
  get: ^4.3.8
  responsive_framework: ^0.1.4
  sizer: ^2.0.15

dev_dependencies:
  build_runner: ^2.1.2
  dart_code_metrics: ^4.3.3
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.3.0

flutter:
  uses-material-design: true
